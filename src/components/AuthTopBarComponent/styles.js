import { StyleSheet } from "react-native";
import { CP } from "~/utils";

export default StyleSheet.create({
  container: {
    borderColor: "#cbcbcb",
    borderBottomWidth: CP.hp(0.2)
  },
  titleStyle: {
    fontSize: 16,
    fontWeight: "700"
  }
});
